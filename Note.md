# 前端知识点

<!-- > test -->

## 1. MVC与MVVM

```
MVC:
- Model: 存放数据模型
- View: 视图层
- Controller: 接受数据请求
当Controller接收到请求后,更新model的数据,model再针对dom进行操作,进行view的更新

MVVM:
- Model
- View
- View Model
View Model接受数据请求,更新model的数据,同时更新view视图,进行dom的渲染,实现了双向绑定的效果
```

## 2. v-model实现双向绑定原理
```
vue2.0是通过Object.defineProperty给对象属性设置get和set方法来实现数据实时更新与展现的
当用户输入发生改变时,首先利用get获取当前值,随后给相应元素的value赋值set更新,最后渲染dom
```

## 3. Vue的生命周期的理解
``` 
Vue的生命周期: 一个Vue的实例从创建到销毁的过程.
- beforeCreate 实例被创建前执行,数据未加载
- created 实例已被创建,初始化数据,dom未加载
- beforeMount 虚拟dom已创建,数据渲染之前,最后一次更改数据
- mounted dom已渲染
- beforeUpdate 重新渲染前触发
- updated 数据已更新,页面重新渲染
- beforeDestroy 实例销毁前,此时实例还存在
- destroyed 实例已销毁
```

## 4. for...in 与 for... of的区别
```
for...in遍历的是数组的索引（即键名），而for...of遍历的是数组元素值
```

## 5. $nextTick的作用
```
用于下次dom循环更新后执行延迟回调
在修改数据之后使用$nextTick可以在回调后获取更新后的dom
```

## 6. JS的基本类型，和引用类型，区别
```
- 基本类型: number，string，boolean，null，undefined
（1）基本类型的访问是按值访问的，就是说你可以操作保存在变量中的实际的值
（2）我们不能给基本类型添加属性和方法
（3）基本类型的变量是存放在栈区的（栈区指内存里的栈内存），栈区包括了 变量的标识符和变量的值。


- 引用类型: function，object，array
（1）引用类型的值是按引用访问的
（2）引用类型可以拥有属性和方法，并且是可以动态改变的。
（3）引用类型的存储需要内存的栈区和堆区（堆区是指内存里的堆内存）共同完成，栈区内存保存 变量标识符 和 指向堆内存中该对象的指针

es6新增原始数据类型，Symbol
保证每个对象的数据属性都是独一无二的，具有唯一性。

```
## 7. Computed和methods区别
```
首先，对于最终的结果，两种方式是相同的，
针对computed，计算属性是基于它们的依赖进行缓存的，只有在其相关依赖改变时才会重新求值。
针对method，只要重新渲染，method调用总会执行该函数
```

## 8. Computed和watch
```
Computed是基于数据之上的对数据的一些计算操作
Watch是当特定的一个数据发生变化时，进行回调操作
```

## 9. Vue中的data为什么必须是函数？
```
对象属于引用类型，当修改其中一个属性时，会影响到所有vue实例的数据。
如果data是一个函数，返回一个对象，那么每一个实例的data对象都是互相独立的，不会相互影响。
```

## 10. html的语义化标签
```
让每个标签都做其正确的事情，
比如，img标签是放置图片的，p标签放置段落等
```

## 11. var, let和const的区别
```
- var 是最原始的变量定义，可以重复声明与赋值没有块级作用域
- let 不可以重复声明，但可以修改值，拥有块级作用域
- const 不可以重复声明与赋值，拥有块级作用域
```

## 12. rem与em的区别
```
- rem root,是根据根节点的font-size大小所确定的，相对长度单位
- em 先根据自身元素的font-size来确定，如若没有定义，便层层向上寻找最近的父元素的font-size来确定
```

## 13. 回调函数中this的指向
```
- 普通函数 function() {} this的值是动态的，实际执行再进行确认
- 箭头函数 () => {} 一般指向window
被函数包含时，指向最近一层的普通函数
取决于外部普通函数里的this的值
```

## 14. CSS动画
```
- transition
- animation
通过@keyframes规则创建动画
--animation-name
--animation-duration
--animation-iteration-count
```

## 15. 删除当前数组元素
```
- splice 会改变原来的数组
- slice 会返回新数组，不会改变原先数组
```
## 16. 数组去重
```
- set
- indexOf，当返回的不是当前元素索引，删去
- 新数组，遍历，已经有的，下一个
```

## 17. $refs
```
对象，拥有所有已注册过ref的子组件
更好地操作dom
```

## 18. 跨域
```
首先，跨域是由于浏览器的同源策略所导致的，就是说浏览器层面导致了跨域。是一个安全机制。
- Jsonp src，img，script等标签是没有跨域限制，利用这一点就可以解决跨域资源请求的问题
- iframe jsonp只能发送get请求，post
- CORS 
  实现CORS通信的关键是服务器，只要服务器实现了CORS接口就可以跨源通信
  浏览器将CORS请求分成两类：简单请求（simple request）和非简单请求（not-so-simple request）。

  只要同时满足以下两大条件，就属于简单请求。 浏览器：
  （1) 请求方法是以下三种方法之一：
    HEAD
    GET
    POST
  （2）HTTP的头信息不超出以下几种字段：
    Accept
    Accept-Language
    Content-Language
    Last-Event-ID
    Content-Type：只限于三个值application/x-www-form-urlencoded、multipart/form-data、text/plain

  浏览器在header中增加origin字段


- 代理 ngix 将请求转发到真正的后端域名




服务端层面
Access-Control-Allow-Origin: *
```

## 19. 服务端渲染
```
server side render
页面上的服务由服务端渲染生成，浏览器直接显示服务端返回的html
```

## 20. Vuex
```
状态管理
- state 驱动应用的数据源
- view 以声明的方式将state映射到view
- actions 响应用户输入再view上的变化
```

## 21. 输入URL后
```
- DNS域名解析
- TCP连接
- HTTP请求
- 处理请求并返回响应
- 页面渲染
    解析html文件，构建DOM树
    解析css，形成css对象模型
    css与dom合并，构建渲染树
    布局和绘制
- 关闭连接
```

## 22. v-for中key的作用
```
- 无key时 默认绑定的是位置
- 有key时，状态根据key的属性值绑定到相应的数组元素
```

## 23. 设计模式
```
- 观察者模式：观察者与目标直接进行交互，由目标直接进行调度，需要有对观察者的处理，造成代码冗余。
- 发布订阅模式：有事件调度中心，实现了解耦，能够实现更细粒度的一些控制，消除发布者与订阅者直接的依赖
```

## 24. 闭包
```
能够读取其他函数内部变量的函数、
将函数内部与函数外部连接起来的桥梁
```

## 25. 作用域链
```
变量从内至上查找
```

## 26. css浮动
```
浮动框不在文档的普通流中，文档普通流的布局就好像浮动框不存在
- float

```

## 27. css选择器优先级
```
- 简单选择器
  行内选择器 > id选择器 > 类选择器...
- 伪元素选择器
- 组合选择器

```

## 性能优化
```
CDN原理

```



## 自我介绍
```
您好，我叫沈昕瑜，来自东华大学，管理科学与工程专业，现在在读研二。在本科阶段接连参加了许多竞赛，比如数学建模比赛，均获得了二等奖以上的成绩。主要的项目有仓库选址系统的网站开发与设计师平台app的开发，网页端页面开发当时利用的asp.net，移动端页面用的Android studio，此时初步接触了前端。
在实习方面，在大三暑假担任哔哩哔哩的B+部门的前端实习生，主要负责管理后台的页面开发，使用的框架是vue，利用element-ui组件进行布局设计，实现页面数据的增删查改，对部门整个研发流程有了初步与基本的认识。在研一的暑假，去了趣头条，同样主要负责toB米读小说管理后台的业务，用到的是vue+element-ui。在研二的寒假，去了普华永道信息技术，将日语学以致用，主要担任软件开发实习生的岗位，接触了从详细设计到前后端开发与测试的任务。
目前，也不断在夯实基础，查漏补缺，希望毕业以后能在前端方向继续发展，最后很荣幸能有这次面试的机会。谢谢。
```